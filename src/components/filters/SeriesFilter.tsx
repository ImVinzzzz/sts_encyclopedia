import type {StarTrekSeries} from '../../types/species';
const series:StarTrekSeries[]=['TOS','TAS','TNG','DS9','VOY','ENT','DIS','PIC','SNW','LD','PRO','FILM'];
export function SeriesFilter({selected,onChange}:{selected:StarTrekSeries[],onChange:(s:StarTrekSeries[])=>void}){return <div><div className="text-[10px] tracking-[.3em] text-sky-300 mb-2">SERIES / PRODUCTIONS</div><div className="flex flex-wrap gap-2">{series.map(s=>{const active=selected.includes(s);return <button key={s} onClick={()=>onChange(active?selected.filter(x=>x!==s):[...selected,s])} className={`px-3 py-1.5 rounded-full text-[10px] font-bold tracking-widest border transition ${active?'bg-[#cc99cc] text-slate-950 border-[#cc99cc]':'bg-slate-900/70 text-slate-400 border-slate-700 hover:border-[#cc99cc]'}`}>{s}</button>})}</div></div>}
